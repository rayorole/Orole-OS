# Server-side only — never exposed to the browser. The app proxies ElevenLabs
# through /api/elevenlabs/* so this key stays on the server.
ELEVENLABS_API_KEY=

# Base URL of the Hermes gateway (OpenAI-compatible). Empty = same origin.
VITE_HERMES_BASE_URL=
